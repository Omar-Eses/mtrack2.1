import 'package:flutter/material.dart';

class ManageTeamMembers extends StatelessWidget {
  const ManageTeamMembers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
